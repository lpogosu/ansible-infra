#jinja2: trim_blocks: True, lstrip_blocks: True
# {{ ansible_managed }}
# Sourced by the backup wrapper with `set -a`. Mode 0600, root only.

RESTIC_REPOSITORY={{ backup_repository }}
RESTIC_PASSWORD_FILE={{ backup_password_file }}
{% for key, value in backup_environment | dictsort %}
{{ key }}={{ value }}
{% endfor %}
