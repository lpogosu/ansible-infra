---
docker_host_repo_prerequisites:
  - ca-certificates
  - curl
  - gnupg
  # deb822_repository parses and writes the .sources file through python-debian;
  # without it the module fails on the target host, not on the controller.
  - python3-debian
docker_host_compose_package: docker-compose-plugin
